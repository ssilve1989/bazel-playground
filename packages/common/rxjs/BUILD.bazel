load("//tools/node:index.bzl", "ts_project")

package(default_visibility = ["//visibility:public"])

ts_project(
    name = "rxjs",
    srcs = glob(["**/*.ts"]),
    deps = [
        "@npm//rxjs",
    ],
)
